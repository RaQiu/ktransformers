--- a/third_party/sglang/python/sglang/srt/multimodal/processors/base_processor.py
+++ b/third_party/sglang/python/sglang/srt/multimodal/processors/base_processor.py
@@ -101,7 +101,8 @@
         if isinstance(token, str):
             return token
-        return processor.tokenizer.convert_ids_to_tokens([token])[0]
+        tokenizer = getattr(processor, "tokenizer", processor)
+        return tokenizer.convert_ids_to_tokens([token])[0]
 
     def convert_to_strs(self, processor):
         if not self.image_token:
